#pragma once

struct vertex_psc
{
};

[factory]
    // static vk_buffer_ver[MAX_BUFFER = 10000];
    // static vk_buffer_ver[MAX_BUFFER = 10000];
    // static vk_buffer_ver[MAX_BUFFER = 10000];

    // angliment_pool

    // buffer_pool<vk_buffer = T, 400>
    //  T allocate(){
    //     size_t offset = sizeof(t)

    //  }
